node_modules
.next
out

# Cloudflare / OpenNext build output
.open-next
.wrangler
cloudflare-env.d.ts
# env files — ignore all secrets, keep the committed template
.env
.env.*
!.env.example
.DS_Store
*.tsbuildinfo
next-env.d.ts

# Local reference material, not for the repo
ref/
